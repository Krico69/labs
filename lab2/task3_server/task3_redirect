<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <title>Завдання 3 — Перенаправлення</title>
    <style>
        body { font-family: Arial, sans-serif; max-width: 500px; margin: 60px auto; background: #f5f5f5; }
        .card { background: #fff; border-radius: 8px; padding: 28px 32px; box-shadow: 0 2px 8px rgba(0,0,0,0.08); }
        h2 { margin-top: 0; color: #333; }
        p { color: #555; }
        button { margin-top: 16px; padding: 10px 24px; background: #4a90e2; color: #fff; border: none; border-radius: 6px; font-size: 1rem; cursor: pointer; }
        button:hover { background: #357abd; }
    </style>
</head>
<body>
<div class="card">
    <h2>Ви потрапили сюди автоматично</h2>

    <p>Сторінка з інформацією про сервер доступна лише після відправки форми (POST-запит).</p>
    <p>Натисніть кнопку нижче щоб відправити POST-запит та побачити дані $_SERVER.</p>

    <form method="POST" action="index.php">
        <button type="submit">Переглянути інформацію про сервер</button>
    </form>
</div>
</body>
</html>
